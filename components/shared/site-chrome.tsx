import Link from "next/link"
import { ArrowUpRight } from "lucide-react"
import type { GlobalContent } from "@/content/schema"
export function SiteNavbar({content}:{content:GlobalContent["nav"]}){return <header className="border-b border-border bg-background"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 md:px-6"><Link href="/" className="text-xl font-bold tracking-tighter text-primary">{content.logo}</Link><nav className="hidden gap-6 md:flex" aria-label="Primary">{content.links.map(l=><Link key={l.href} href={l.href} className="text-sm">{l.label}</Link>)}</nav><Link href={content.cta.href} className="rounded-full bg-primary px-6 py-3 text-sm text-primary-foreground">{content.cta.label}</Link></div></header>}
export function SiteFooter({cta,content}:{cta:GlobalContent["footerCta"],content:GlobalContent["footer"]}){return <><section className="bg-ink px-5 py-20 text-primary-foreground"><div className="mx-auto max-w-4xl text-center"><p className="text-xs uppercase">Ready to work together?</p><h2 className="mx-auto mt-6 max-w-2xl font-serif text-5xl leading-none md:text-7xl">{cta.heading}</h2><div className="mt-10 grid gap-4 sm:grid-cols-2"><Link href={cta.primaryCta.href} className="rounded-full bg-background px-8 py-4 font-semibold text-foreground">{cta.primaryCta.label}</Link><Link href={cta.secondaryCta.href} className="rounded-full border border-primary-foreground px-8 py-4 font-semibold">{cta.secondaryCta.label}</Link></div></div></section><footer className="bg-ink px-5 pb-10 text-primary-foreground"><div className="mx-auto grid max-w-7xl gap-10 border-t border-primary-foreground/20 pt-12 md:grid-cols-3"><p className="max-w-sm text-sm text-primary-foreground/60">{content.description}</p><div><p className="footer-label">Based in</p><p className="mt-3">{content.location}</p><a className="mt-6 block" href={`mailto:${content.contact.email}`}>{content.contact.email}</a><a className="mt-2 block" href={`tel:${content.contact.phone}`}>{content.contact.phone}</a></div><div className="flex flex-col gap-2">{content.social.map(s=><a key={s.platform} href={s.href} className="flex items-center gap-1">{s.platform}<ArrowUpRight size={14}/></a>)}</div></div><p className="mx-auto mt-12 max-w-7xl text-xs text-primary-foreground/50">{content.copyright}</p></footer></>}
